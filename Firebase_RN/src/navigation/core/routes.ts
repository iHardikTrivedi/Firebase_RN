// Root stack routes
export enum RootRoutes {
  Splash = 'Splash',
  Auth = 'Auth',
  Tab = 'Tab',
}

// Auth stack routes
export enum AuthRoutes {
  SignIn = 'SignIn',
  SignUp = 'SignUp',
}

// Tab stack routes
export enum TabRoutes {
  Home = 'Home',
  Profile = 'Profile',
}
